"""NovoExplorer test suite.

Uses pytest with shared fixtures defined in ``conftest.py``.  Each test
module corresponds to a pipeline or plotting module (e.g.
``test_ingest.py`` tests ``pipeline/ingest.py``).
"""