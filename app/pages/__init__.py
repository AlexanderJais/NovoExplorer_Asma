"""Streamlit pages for the NovoExplorer multi-page app.

Each module defines a ``main()`` function that renders one page.  Pages
are registered in ``app/app.py`` and share components from
``app/components/``.
"""